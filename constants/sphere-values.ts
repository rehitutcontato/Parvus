/**
 * Frames SVG para `<animate attributeName="d">` (morphing), estilo 21st.dev.
 * Deixe vazio para usar o modo wireframe (animateTransform) em `spinning-sphere.tsx`.
 * Se tiver o string completo do componente original, cole aqui entre backticks.
 */
export const SPHERE_VALUES = ""
